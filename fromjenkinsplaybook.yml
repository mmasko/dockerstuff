---
- hosts: localhost
  gather_facts: false
  
  tasks:

    - name: Jenkins runs me
      debug:
        msg: "Hello world"

    - name: create a file
      copy:
        content: 'Jenkins made me'
        dest: /home/ec2-user/jenkins.txt